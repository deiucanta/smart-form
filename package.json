{
  "name": "smart-form",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter demo dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "registry:build": "shadcn build apps/demo/registry.json -o public"
  },
  "devDependencies": {
    "shadcn": "^2.3.0",
    "typescript": "~5.9.3"
  }
}
